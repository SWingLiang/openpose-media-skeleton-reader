# Release Notes v0.3.1

- 副标题保持一行，并提示完整操作流程。
- Python 连接状态不再做成按钮形状。
- OpenPoseDemo.exe 和 models 文件夹增加本地选择按钮。
- 支持直接打开已有 OpenPose JSON 输出文件夹。
- 新增显示原视频/图片开关，可只显示骨架、关键点或编号。
- Audio/RGB 面板改为固定高度，避免页面过长。
- Audio 改为播放后实时连接视频音轨做 waveform/spectrum。
- 保留 BODY_25、Hand、Face 叠加显示开关。
